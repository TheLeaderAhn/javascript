Math.pow(2.53); // 2의 53 제ㅂ
Math.round(0.6); // 가장 가까운 정수로 올림
Math.ceil(0.6); // 정수로 올림
Math.floor(0.6); // 0.0 정수로 내림
Math.max(x, y, z); // 인자중 가장 큰값
Math.min(x, y, z); // 인자값중 가장 작은값 반환
Math.random(); // 0~1 사이 임의의 수
Math.PI; // 원주율 3.1415926535
Math.E; // 자연로그 e
Math.sqrt(3); // 루트3
Math.pow(3, 1 / 3);
Math.sin(0); // 사인 0- 삼각함수
Math.log(10); //log10
Math.exp(3); // e^3

Math.cbrt(27); // 인자 세제곱근 -> 3
Math.hypot(3, 4); // 인자의 제곱의 합의 제곱근 ? 삼각함수 값인듯
Math.imul(2, 3); // 6 : 32qlxm wjdtndml wprhq
Math.trunc(3.9); // 소수점 잘라내고 3반환

// Bigint로 이믜 정확도를 부여한 정수
